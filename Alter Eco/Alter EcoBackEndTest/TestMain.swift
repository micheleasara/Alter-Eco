import Foundation
import UIKit

// This is the test bundle entry point. An entry point is needed to run any program.
// Tests and app have been separated to gather coverage for backend files only
@UIApplicationMain
class AppDelegate: UIResponder, UIApplicationDelegate {}
